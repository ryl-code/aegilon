services:


  backend:
    build: ./Aegilon/backend
    container_name: aegilon-backend

    ports:
      - "8080:8080"
    volumes:
      - ./Aegilon/backend:/app
    env_file:
      - ./Aegilon/.env
    networks:
      - wazuh_net
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8080/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s

  detection-engine:
    build: ./Aegilon/detection-engine
    container_name: aegilon-detection-engine
    volumes:
      - ./Aegilon/detection-engine:/app
    env_file:
      - ./Aegilon/.env
    networks:
      - wazuh_net
    depends_on:
      backend:
        condition: service_healthy

  risk-engine:
    build: ./Aegilon/risk-engine
    container_name: aegilon-risk-engine
    volumes:
      - ./Aegilon/risk-engine:/app
    env_file:
      - ./Aegilon/.env
    networks:
      - wazuh_net
    depends_on:
      backend:
        condition: service_healthy

  automation-engine:
    build: ./Aegilon/automation-engine
    container_name: aegilon-automation-engine
    volumes:
      - ./Aegilon/automation-engine:/app
    env_file:
      - ./Aegilon/.env
    networks:
      - wazuh_net
    depends_on:
      backend:
        condition: service_healthy

  frontend:
    build: ./Aegilon/frontend
    container_name: aegilon-frontend
    ports:
      - "3002:3000"
    environment:
      - BACKEND_PROXY_TARGET=http://backend:8080
    networks:
      - wazuh_net
    depends_on:
      backend:
        condition: service_healthy

networks:
  wazuh_net:
    name: single-node_default
    external: true
